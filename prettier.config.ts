import type { Config } from "prettier";

const config: Config = {
  endOfLine: "auto",
  printWidth: 100,
  singleAttributePerLine: true,
  tabWidth: 2,
  trailingComma: "es5",
};

export default config;
